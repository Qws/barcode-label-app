{
  "name": "label-printer",
  "version": "1.0.0",
  "description": "Converts HTML to PDF for labels",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deno suresan",
  "license": "ISC",
  "dependencies": {
    "barcode": "^0.1.0",
    "html-pdf": "^2.2.0"
  }
}
